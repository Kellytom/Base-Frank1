---
title: "FF2: Responsive Menu Demo"
---

<!-- Responsive Menu with Submenus (from template) -->
<nav class="navbar-bar">
  <div class="navbar-container">
    <div class="navbar-left">
      <span class="navbar-brand">Base-Frank1</span>
      <a href="/map" class="navbar-map-link" style="display:inline-flex;align-items:center;margin-left:0.5em;text-decoration:none;color:#e8d5b7;font-weight:bold;">
      <a href="/map" class="navbar-map-link">
        <span class="map-btn">
          <i class="fa fa-power-off" aria-hidden="true"></i>
          <span style="margin-left:0.4em;">Map</span>
        </span>
      </a>
    </div>
    <button class="hamburger" id="hamburger-btn" aria-label="Open menu" aria-expanded="false" aria-controls="main-menu">
      <span></span>
      <span></span>
      <span></span>
    </button>
    <select class="mode-select desktop-only" aria-label="Theme mode">
      <option value="light">Light</option>
      <option value="sunset">Sunset</option>
      <option value="dark">Dark</option>
      <option value="system">System</option>
    </select>
    <form class="search-form desktop-only" role="search">
      <input type="search" placeholder="Search..." aria-label="Search" />
    </form>
    <!-- Mobile menu -->
    <ul class="menu" id="main-menu">
      <li class="mobile-only">
        <select class="mode-select" aria-label="Theme mode">
          <option value="light">Light</option>
          <option value="sunset">Sunset</option>
          <option value="dark">Dark</option>
          <option value="system">System</option>
        </select>
      </li>
      <li class="mobile-only">
        <form class="search-form" role="search">
          <input type="search" placeholder="Search..." aria-label="Search" />
        </form>
      </li>
      <li class="dropdown">
        <button class="dropdown-toggle" tabindex="0">
          <span>Designs</span>
        </button>
        <ul class="submenu dropdown-menu">
          <li><a href="/backgrounds" class="dropdown-item">Backgrounds</a></li>
          <li><a href="/daisyui-showcase" class="dropdown-item">DaisyUI</a></li>
          <li><a href="/starlight-css-guide" class="dropdown-item">CSS Guide</a></li>
          <li><a href="/bulma" class="dropdown-item">Bulma Demo</a></li>
          <li><a href="/ff2-navbar-demo" class="dropdown-item">🚀 FF2 Navbar Demo</a></li>
          <li><a href="/frames" class="dropdown-item">Frames</a></li>
          <li><a href="/frames-pale" class="dropdown-item">Frames Pale</a></li>
        </ul>
      </li>
      <li class="dropdown">
        <button class="dropdown-toggle" tabindex="0">
          <span>Menu 2</span>
        </button>
        <ul class="submenu">
          <li><a href="/menu2/sub1">Submenu 2-1</a></li>
          <li><a href="/menu2/sub2">Submenu 2-2</a></li>
          <li><a href="/menu2/sub3">Submenu 2-3</a></li>
          <li><a href="/menu2/sub4">Submenu 2-4</a></li>
          <li><a href="/menu2/sub5">Submenu 2-5</a></li>
        </ul>
      </li>
      <li class="dropdown">
        <button class="dropdown-toggle" tabindex="0">
          <span>Menu 3</span>
        </button>
        <ul class="submenu">
          <li><a href="/menu3/sub1">Submenu 3-1</a></li>
          <li><a href="/menu3/sub2">Submenu 3-2</a></li>
          <li><a href="/menu3/sub3">Submenu 3-3</a></li>
          <li><a href="/menu3/sub4">Submenu 3-4</a></li>
          <li><a href="/menu3/sub5">Submenu 3-5</a></li>
        </ul>
      </li>
      <li class="dropdown">
        <button class="dropdown-toggle" tabindex="0">
          <span>Menu 4</span>
        </button>
        <ul class="submenu">
          <li><a href="/menu4/sub1">Submenu 4-1</a></li>
          <li><a href="/menu4/sub2">Submenu 4-2</a></li>
          <li><a href="/menu4/sub3">Submenu 4-3</a></li>
          <li><a href="/menu4/sub4">Submenu 4-4</a></li>
          <li><a href="/menu4/sub5">Submenu 4-5</a></li>
        </ul>
      </li>
      <li class="dropdown">
        <button class="dropdown-toggle" tabindex="0">
          <span>Play</span>
        </button>
        <ul class="submenu">
          <li><a href="/play/paper">Paper</a></li>
          <li><a href="/play/computer">Computer</a></li>
          <li><a href="/play/real-world">Real World</a></li>
        </ul>
      </li>
      <li class="dropdown">
        <button class="dropdown-toggle" tabindex="0">
          <span>Work</span>
        </button>
        <ul class="submenu">
          <li><a href="/work/cx">CX</a></li>
          <li><a href="/work/gr">GR</a></li>
          <li><a href="/work/jp">JP</a></li>
          <li><a href="/work/etc">Etc.</a></li>
        </ul>
      </li>
      <li class="dropdown">
        <button class="dropdown-toggle" tabindex="0">
          <span>...</span>
        </button>
        <ul class="submenu">
          <li><a href="#future">future expansion</a></li>
          <li><a href="#experimental">experimental</a></li>
          <li><a href="#untested">untested</a></li>

          <li><a href="/Attribution/">Attrib</a></li>
        </ul>
      </li>
    </ul>
  </div>
</nav>

<main style="padding: 2rem; max-width: 800px; margin: 0 auto; margin-top: 70px;">
 
</main>

<style>
body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  font-size: 0.85rem;
  margin: 0;
  padding: 0;
  background: #f4f4f4;
}
.navbar-bar {
  width: 100%;
  background: rgba(60, 40, 80, 0.6); /* dark muted purple, 60% opacity */
  box-sizing: border-box;
  z-index: 3000;
  position: relative;
}
.navbar-container {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  gap: 0.7rem;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 1rem;
  flex-wrap: nowrap;
  min-width: 0;
}
.navbar-left {
  display: flex;
  align-items: center;
  flex-shrink: 1;
  min-width: 0;
}
.navbar-brand {
  color: #fff;
  font-size: 1.5em;
  font-weight: bold;
  margin-right: .2rem;
  letter-spacing: 1px;
  display: flex;
  align-items: center;
  height: 100%;
}
.menu {
  display: flex;
  flex-wrap: nowrap;
  list-style: none;
  margin: 0;
  padding: 0;
  flex-shrink: 1;
  min-width: 0;
  background: rgba(60, 40, 80, 0.6); /* dark muted purple, 60% opacity */
  backdrop-filter: blur(2px);
}
.menu > li {
  min-width: 0;
}
.dropdown {
  position: relative;
}
.menu > li > a,
.dropdown-toggle,
.submenu li a {
  color: #e8d5b7;
  font-size: 1rem;
  border-bottom: 3px solid #b8956a;
  padding-bottom: 0.5rem;
  text-align: left;
  font-family: inherit;
}
.menu > li > a:hover,
.menu > li:focus-within > a {
  background: #444;
}
.dropdown-toggle {
  background: none;
  border: none;
  color: #fff;
  font: inherit;
  padding: 1em 1.5em;
  cursor: pointer;
  transition: background 0.2s;
}
.dropdown-toggle:hover,
.dropdown-toggle:focus {
  background: #444;
}
.submenu li a {
  display: block;
  padding: 0.75em 1.5em;
  color: #e8d5b7;
  text-decoration: none;
  transition: background 0.2s;
  background: rgba(60, 40, 80, 1); /* solid dark muted purple for submenu items */
  border-radius: 6px;
  margin: 2px 8px;
}
.submenu li a:hover {
  background: #555;
}
.navbar-right {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-left: 2rem;
}
.search-form input[type="search"] {
  width: 90px;
  transition: width 0.3s cubic-bezier(0.4,0,0.2,1);
  padding: 0.5em 1em;
  border-radius: 4px;
  border: none;
  font-size: 1em;
  outline: none;
}
.search-form input[type="search"]:focus,
.search-form input[type="search"]:not(:placeholder-shown) {
  width: 180px;
}
.mode-select {
  padding: 0.2em 0.7em;
  border-radius: 4px;
  border: none;
  font-size: 0.95em;
  background: #444;
  color: #fff;
  height: 2em;
  min-width: 80px;
  line-height: 1.2;
  margin: 0 0.5em 0 0.5em;
  transition: background 0.3s;
}
.mode-select option {
  color: #222;
  background: #fff;
}
body[data-theme="dark"] .mode-select,
body[data-theme="dark"] .mode-select option {
  color: #fff;
  background: #222;
}
body[data-theme="sunset"] .mode-select,
body[data-theme="sunset"] .mode-select option {
  color: #5a3a1b;
  background: #f5e6d3;
}
body[data-theme="dark"] {
  background: rgba(34,34,34,0.8);
}
body[data-theme="sunset"] {
  background: #f5e6d3;
}
.hamburger {
  display: none;
  flex-direction: column;
  justify-content: center;
  width: 2.5em;
  height: 2.5em;
  cursor: pointer;
  margin-left: 1em;
  z-index: 2000;
  background: none;
  border: none;
}
.hamburger span {
  display: block;
  height: 4px;
  width: 28px;
  background: #fff;
  margin: 5px 0;
  border-radius: 2px;
  transition: 0.3s;
}
@media (max-width: 800px) {
  .navbar-container {
    flex-direction: column;
    align-items: stretch;
    gap: 0.5rem;
  }
  .navbar-right {
    margin-left: 0;
    justify-content: flex-end;
  }
  .hamburger {
    display: flex;
  }
  .menu {
    display: none;
    flex-direction: column;
    width: 100%;
    background: rgba(60, 40, 80, 0.9); /* dark muted purple, 90% opacity for mobile */
    position: absolute;
    left: 0;
    top: 60px;
    z-index: 1500;
    backdrop-filter: blur(2px);
  }
  .menu.menu-open {
    display: flex;
  }
  .navbar-container {
    position: relative;
  }
  .mobile-only {
    display: block;
    width: 100%;
    padding: 0.5em 1em;
  }
  .mode-select, .search-form {
    width: 100%;
    margin: 0.5em 0;
  }
  /* Hide all submenus by default on mobile */
  .submenu {
    display: none;
  }
  /* Show submenu only when parent .dropdown has .open */
  .dropdown.open > .submenu {
    display: flex;
    flex-direction: column;
    position: static;
    background: rgba(83, 70, 95, 0.9); /* dark muted purple, 90% opacity for mobile */
    min-width: 180px;
    list-style: none;
    margin: 0;
    padding: 0;
    z-index: 1000;
    backdrop-filter: blur(2px);
  }
}
@media (min-width: 801px) {
  .mobile-only {
    display: none !important;
  }
  .desktop-only {
    display: inline-block;
  }
  .submenu {
    display: none;
    position: absolute;
    left: 0;
    top: 100%;
    background: rgba(72, 64, 80, 0.9); /* dark muted purple, 90% opacity */
    min-width: 180px;
    list-style: none;
    margin: 0;
    padding: 0;
    z-index: 1000;
    backdrop-filter: blur(2px);
  }
  /* Removed hover/focus logic for desktop submenus */
  .dropdown.open > .submenu {
    display: block;
  }
}
.navbar-left {
  display: flex;
  align-items: center;
}
.navbar-map-link {
  margin-left: 0.7em;
  text-decoration: none;
}
.map-btn {
  display: inline-flex;
  align-items: center;
  background: #444;
  color: #e8d5b7;
  border-radius: 6px;
  padding: 0.3em 0.9em;
  font-weight: bold;
  font-size: 1em;
  border: 2px solid #b8956a;
  box-shadow: 0 1px 4px rgba(0,0,0,0.08);
  transition: background 0.2s, color 0.2s;
}
.map-btn:hover {
  background: #b8956a;
  color: #222;
}
.map-btn i.fa-power-off {
  font-size: 1.1em;
  margin-right: 0.3em;
}
</style>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

<script>
// Hamburger menu toggle (vanilla JS)
document.addEventListener('DOMContentLoaded', function() {
  const hamburger = document.getElementById('hamburger-btn');
  const menu = document.getElementById('main-menu');
  if (hamburger && menu) {
    hamburger.addEventListener('click', function(e) {
      // Only toggle main menu, not submenus
      const isOpen = menu.classList.toggle('menu-open');
      hamburger.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
      // On mobile, close all submenus when menu is opened
      if (isMobile()) {
        document.querySelectorAll('.dropdown.open').forEach(function(el) {
          el.classList.remove('open');
        });
      }
    });
  }
  // Light/Dark/Sunset mode switcher
  const modeSelect = document.querySelector('.mode-select');
  function setTheme(theme) {
    document.body.setAttribute('data-theme', theme);
  }
  if (modeSelect) {
    modeSelect.addEventListener('change', function(e) {
      const target = e.target;
      if (target) setTheme(target.value);
    });
    // Set initial theme
    if (modeSelect.value) {
      setTheme(modeSelect.value);
    }
  }
  // Mobile/Desktop submenu toggle
  function isMobile() {
    return window.innerWidth <= 800;
  }
  function closeAllDropdowns() {
    document.querySelectorAll('.dropdown.open').forEach(function(el) {
      el.classList.remove('open');
    });
  }
  const dropdownToggles = document.querySelectorAll('.dropdown-toggle');
  dropdownToggles.forEach(function(btn) {
    btn.addEventListener('click', function(e) {
      e.preventDefault();
      const parent = btn.parentElement;
      if (parent && parent.classList.contains('open')) {
        parent.classList.remove('open');
      } else if (parent) {
        closeAllDropdowns();
        parent.classList.add('open');
      }
    });
  });
  // Close dropdowns when clicking outside (desktop only)
  document.addEventListener('click', function(e) {
    if (!isMobile()) {
      const isDropdown = e.target.closest('.dropdown');
      const isToggle = e.target.closest('.dropdown-toggle');
      if (!isDropdown && !isToggle) {
        closeAllDropdowns();
      }
    }
  });
  // Optional: close dropdowns when resizing to desktop
  window.addEventListener('resize', function() {
    if (!isMobile()) closeAllDropdowns();
  });
});
</script>
